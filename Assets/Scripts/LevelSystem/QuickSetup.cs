using UnityEngine;

/// <summary>
/// 原本用來一次幫 LevelManager + WaveManager 做快速設定的工具。
/// 現在整個專案改用 `GameManager + SimpleLevelController`，這個腳本不再需要，
/// 保留為空殼以避免場景上掛著舊組件時出現編譯錯誤。
/// </summary>
public class QuickSetup : MonoBehaviour
{
    // 目前不做任何事。
}
